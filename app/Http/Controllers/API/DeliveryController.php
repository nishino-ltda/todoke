<?php

namespace App\Http\Controllers\API;

use App\Http\Controllers\Controller;

class DeliveryController extends Controller
{
    // This controller is kept as a placeholder since its methods have been
    // split into more specialized controllers:
    // - DeliveryManagementController (index, store, show)
    // - DeliveryStatusController (accept, updateStatus) 
    // - DeliveryMessagingController (storeMessage, indexMessages)
    // - DeliveryHelpersTrait (shared helper methods)
}
